# Extras

Unmaintained optional components that are not part of the shipped Docker image.

- `receipt_gui.py` — legacy desktop GUI. Requires `pip install customtkinter`
  (not in requirements.txt). The web UI (`server.py`) supersedes it.
