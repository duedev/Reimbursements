# Lightweight test/CI dependencies.
#
# The local OCR stack in requirements.txt (rapidocr-onnxruntime + onnxruntime) is
# lazy-imported and NOT exercised by the test suite — the engine and its result
# parsing are unit-tested with mocks/synthetic output — so CI installs only what
# the tests actually import. The full runtime stack still ships in the Docker image.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
openai>=1.57.0
openpyxl>=3.1.5
Pillow>=11.0.0
PyMuPDF>=1.24.0
httpx>=0.27.0
numpy>=1.26.0
pytest>=8.0.0
