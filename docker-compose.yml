services:
  receipt-processor:
    build: .
    ports:
      - "8000:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - LMSTUDIO_BASE_URL=http://host.docker.internal:1234/v1
