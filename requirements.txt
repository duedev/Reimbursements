fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
openai>=1.57.0
openpyxl>=3.1.5
Pillow>=11.0.0
PyMuPDF>=1.24.0
# Local OCR fallback when the LM Studio OCR stage is unreachable (heavy: ~600MB)
# Pin all three to the same major.minor: paddleocr's own paddlex dependency is
# unbounded upstream, and paddlex >= 3.1 made PaddlePredictorOption.__init__
# keyword-only, which breaks paddleocr 3.0.x engine init with
# "PaddlePredictorOption.__init__() takes 1 positional argument but 2 were given".
paddlepaddle==3.0.*
paddleocr==3.0.*
paddlex==3.0.*
# paddlex eagerly imports langchain.docstore for a retriever the receipts fallback
# never uses; langchain>=1.0 removed that module, which would make `import paddleocr`
# fail with "No module named 'langchain.docstore'". process_receipts shims those
# imports at engine-init time, so no langchain pin is required here.
