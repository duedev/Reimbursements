fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
openai>=1.57.0
openpyxl>=3.1.5
Pillow>=11.0.0
PyMuPDF>=1.24.0
# Local OCR fallback when the LM Studio OCR stage is unreachable (heavy: ~600MB)
# Pin both to the same major.minor to avoid PaddlePredictorOption API drift.
paddlepaddle==3.0.*
paddleocr==3.0.*
